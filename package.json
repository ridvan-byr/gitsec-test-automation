{
  "name": "testautomation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:daily": "playwright test --grep @daily",
    "test:weekly": "playwright test --grep @weekly",
    "test:monthly": "playwright test --grep @monthly",
    "test:cron": "playwright test --grep @cron",
    "report": "playwright show-report",
    "mail:github-otp": "tsx scripts/peek-github-otp.ts",
    "mail:check": "tsx scripts/check-github-mail.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0",
    "dotenv": "^16.6.1",
    "tsx": "^4.22.0"
  },
  "dependencies": {
    "imapflow": "^1.3.3",
    "otpauth": "^9.5.1"
  }
}
